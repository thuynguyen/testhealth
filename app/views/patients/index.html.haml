= select_tag 'examine_types', options_for_select(({0 => "All"}).merge(Examine::TYPES).invert)
#list-patients
  = render "list", :patients => @patients

%br
= render "import_form"
= link_to 'New Patient', new_patient_path


= link_to "Import", "#", :id => "link-import"


